CODE: 

#include <stdio.h>

int main() {
    int n;
    printf("Enter number of terms: ");
    scanf("%d", &n);
    if (n <= 0) return 0;
    int a = 0, b = 1;
    printf("Fibonacci series: ");
    for (int i = 1; i <= n; i++) {
        printf("%d", a);
        if (i != n) printf(" ");
        int next = a + b;
        a = b;
        b = next;
    }
    return 0;
}
Input:
8
Output

Enter number of terms: Fibonacci series: 0 1 1 2 3 5 8 13
